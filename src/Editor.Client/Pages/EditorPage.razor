@page "/editor"
@page "/editor/{DocumentId}"
@rendermode @(new InteractiveWebAssemblyRenderMode(prerender: false))

<PageTitle>Diagram Studio Editor</PageTitle>

<EditorShell DocumentId="@DocumentId" />

@code {
    [Parameter]
    public string? DocumentId { get; set; }
}
