# Ghostagram F003 source integration checkpoint

Prototype run PROTOTYPE-RUN-20260907T094049Z; parent owns state/run/closure. Child scope Ghostagram only. No Git/dependency-version/external mutations. System currently owns shared build slot; do not build/test/restore until parent releases a new exact System generation.

## Preserved F004 delivery

F004 source is frozen in ../FEATURE-004/checks/candidate-release/source.zip (from this Feature directory use ../../FEATURE-004/checks/candidate-release/source.zip). Fingerprint 41a988f64d4c7ed22590ef1ddc51c131b85d4675b82f5db6ed45f43a59fb470d; archive SHA8b0cb2995a2c02a2cc83ebf35d0c30b628f17f701505ddd3ce2b4e077857e6fd. All7 ordinary and6 conformance commands pass;15System owners and72copies matched. The old D3 binary had no PublicView; separately executed foundation tests passed against that old shape. These tests do not validate ongoing F003 source.

Elon accepted F004 Solution/Bridge Target3 and Design2. Independent local Validation is now parent-coordinated. Do not modify F004 evidence/review artifacts except exact explicit parent metadata corrections or unchanged transcription of reviewer-authored Validation.

## Current source ownership

Agent ghostagram_runtime_mapping owns src/Ghostagram.Bridge/DeclarativeCompositionProjection and tests/Ghostagram.Bridge.Tests/DeclarativeComposition. It is implementing projector, closed union, exact anchor/typed metadata converters, freshness/delta, bounded inert hierarchy and actual compiler fixtures. Public helper signatures promised: ProjectionFixtures.CompileNested(long revision=1,TimeProvider? timeProvider=null), CompileUnsupported/CompileSkewed/CompileFailed(TimeProvider?=null), Profile(), Context(...). Root will link shared fixture source into conformance rather than duplicate setup.

Root owns tests/Ghostagram.Graph.Conformance/Composition, conformance Program/csproj and tests/Ghostagram.Layout.Verification/CompositionPreview.cs plus Program/csproj wiring. Protocol helper writes the System-owned consumer manifest/envelope shapes; it does not fabricate or approve results. CLI case execution and raw producer import remain to wire. Preview is implemented source-only with --composition-preview <actual result JSON> --output <fresh directory>; derives real subclass JSON with string enum statuses, uses existing SvgDiagramExporter, preserves raw SVG and escaped source/status/diagnostics, shows Stale last-known output explicitly and never renders a new diagram for Failed/Unsupported/Skewed. It records visibleInspection Pending; no visuals yet generated or inspected.

## Actual System evolution

CompositionIr.PublicView now mandatory, compiler-derived before canonical bytes/identity and hash-included. Public component fields retain source Definition identity/revision, occurrence/owner/parent/publicOwnerPath, visibility/provenance/exportChain/IsOpaque, Children/Surfaces and PublicRequirements/PublicAssignments/PublicImplementationReferences. Surfaces retain Declaration plus source Definition revision, not invented surface revision. No raw/private Definitions traversal or service-scope visibility inference.

D3 ComponentDefinition/DefinitionProvenance/ImplementationReference.Metadata is now IReadOnlyDictionary<SemanticAddress,GraphSemanticValue>; explicit typed presentation conversion required. ComponentDefinition visibility defaults Public; occurrence/implref/assignment visibility defaults Private with explicit owner/provenance for Public assignment. System raw compile API and owner implementation source exists. Source-only public IR constructor trust/invariant concern relayed to parent; consumer cannot invent semantic admission.

ConsumerContracts.cs lives in ../ghostworx-system/tests/Ghostworx.System.Composition.Conformance.Support (not src). Real IConsumerEnvelopeAdmission accepts candidate, AdmittedCorpusSnapshot, ProducerResultEnvelope, ConsumerCaseManifest. System owns manifest/envelope schema and raw producer-envelope import seam, latter still being implemented jointly for SDK/Ghostagram. Do not implement a competing consumer producer parser. Source fixture decoder/application mapping also pending System.

## Next

Finish case/scenario execution, materialize/freeze concrete manifest and all named Design cases, wire owner raw producer admission and real fixture decoder, then use the new parent-issued System binaries for ordered child-only cache restore/build/checks. Preserve old F004 generation. Produce real projection JSON and admitted consumer envelope; run actual SVG preview for nested/inert/rejected/stale/unsupported/skewed results and inspect visibly. All7 F003 criteria/evidence/independent Validation remain open until real execution proves them.


## Resume update after local independent F004 validation

F004 VALIDATION.md was mechanically transcribed unchanged from the user-named independent elon-musk reviewer via parent. SHA256 79df10793ce7231ed1c29ffcdd764522a5c80ed54a7813fd414a4cedb7e83c7c; direct readback matched 17,180 characters including final newline. Initial local cycle0 Accepted, original generation only. Reviewer addendum confirms System opacity defect; integrated F004 is not Accepted and requires corrected generation checks. No original frozen F004 archive/Evidence changes.

Parent issued provisional System F001 generation checks/provisional-20260907T105241Z/receipt.json (under System F001), owner manifest SHA06eff16f6aa102679cdbff8ba70404396f32ce7df994c8d01a48db17f03ec8ca, source02a932982de0b0a11ebc11c347c432b7374dcd037511a78e3b72ee3f2963e401. SDK owns shared build slot; Ghostagram next. This generation is diagnostic only: subsequent System cycle1 review found opaque stored metadata bypasses runtime depth16; final repair2 underway. Cannot close F004 against this provisional generation. Do not build System references.

Mapper agent source complete in owned two dirs; added CompositionProjectionJson public display serializer, immutable public provenance/lineage anchors, shared ProjectionFixtures, ProjectionBoundaryCases (157 inventory-only concrete cases, ExecuteCase returns actual operation/outcome/optional result), ProjectionOutputSizeCases (two real Compile/Project calibrated 1MiB/+1 cases; explicit failure if calibration cannot reach boundary). Root wired all4 test Run methods, Compiler project ref, shared fixture/boundary/output-size source links. Nothing built/tested yet against new generation.

Root conformance source now includes CompositionConsumerProtocol, CompositionProjectionScenarios (15 named actual local cases), CompositionConsumerSession, CompositionRunner. Session uses owner CorpusAdmission, ProducerEnvelopeReader, ConsumerEnvelopeAdmission; two-phase materialize/run checks externally supplied frozen manifest digest and exact current fixture bytes/inventory; never fabricates producer output. Common observation fields include actual status/result digest/sourceDigest/diagnosticCodes/assertion/localObservation, optional no-diagram admission-only outcomes. Full producer fixture decoder/applicability registry and final CLI materialize/conformance wiring still missing; owner must supply actual decoder. Local CLI available source: --profile composition --local-projections --output freshdir. Program only intercepts profile value composition or local flag; preserves existing EPIC001 feature003 --profile path. Local output explicitly does not claim producer-linked conformance.

New integration-checks.py in this directory verifies exact parent owner manifest+15owner DLLs before/after, per-test copied System DLLs, child-only topological18project Release builds/cache-only restore, bounded owned subprocesses/native-vs-forced exits, separate phase output folders. Syntax/XML links/git diff --check passed; NO dotnet checks have executed. Invoke only on explicit parent slot grant: python .../integration-checks.py --phase unique-name --generation .swe/implementations/EPIC-002/FEATURE-001/checks/provisional-20260907T105241Z --manifest-sha256 06eff16f6aa102679cdbff8ba70404396f32ce7df994c8d01a48db17f03ec8ca --build --packages C:/Users/justin/.nuget/packages. Tests --test composition|ordinary|conformance|local-projections; --projects selective rebuild names. Provisional results cannot become accepted final evidence.


## First provisional diagnostic slot completed and RELEASED

Parent granted short105241Z slot. Child-only cache restore succeeded and all18local projects built after fixing CompositionPreview Program missing normal-path return0. Captured150-file initial source archive fingerprint d73633b367d7f659c86e277fd40c26c2ea8e4e82c498405c48995a509a39b06a at provisional-build-01. All7ordinary suites and3existing conformance suites nativePASS,15Systemowners and each suite's copied System DLLs matched. See provisional-ordinary-01, provisional-conformance-01 commands/copies/owners JSON. These are provisional diagnostics, not F004finalvalidation.

Composition foundation passed. FirstProject originally failed GRAM-COMP-INVALID because STJ eagerly reflects D3 Composition.OpaqueExtension ReadOnlySpan<byte> ctor even empty lists. Test-only FirstChanceException trace isolated it (provisional-debug-build), then removed. Narrow ExtensionWriter in CompositionPresentationJson mirrors owner display fields identity/isInert/base64payload. Scoped Bridge/Bridge.Tests/conformance rebuild passed, firstNestedProject/refresh/unsupported/skew checks progressed, then actual CompileFailed fixture threw in DefinitionSourceSet(root,[]) ctor before compiler. Mapper agent is correcting fixture SOURCE ONLY to retain valid root but omit required nested definition so actual compiler returns rejected result. No synthetic source result.

Shared slot explicitly RELEASED to parent; no subprocess remains. NO further build/test/restore until next exact generation+slot. System finalrepair2 underway for opaque metadata depth16 bypass and raw optional-memory null preservation;105241Z cannot closeF004. Source fixture decoder still owner-pending. Full composition suite/174localcases/output-sizecalibration/real consumer envelope/visible SVG remain unverified.

Integration runner enhanced for FUTURE phases to capture source archive/fingerprint and per-suite local Ghostagram DLL hashes. This does not retroactively claim those extra captures for previous phases. Preserve all previous failures/logs and original F004archive/Validation.


## Owner fixture decoder and full CLI source integration

System supplied source-only CompositionFixtureRequestDecoder.cs / FIXTURE-REQUESTS.md in tests/Ghostworx.System.Composition.Conformance.Support. Current API corpus.Fixtures map of admitted realized anchors/bytes; Decode(corpus,fixtureId,admittedProfile,admittedContext) returns exact raw CompileRequest or nullable DocumentBytes for Decode/Verify, never executes source operations. Old recipes give empty map and cannot replay. Actual realized corpus remains owner-pending.

New ProducerProjectionCases.cs uses that actual decoder, checks complete retained-case anchor coverage, resolves administrative candidate paths from admitted realized-fixtures-v1.json, retains fixture anchor/digests/actual owner ops/status in Project observations. Compile/Verify supported; Decode executes actual Decode then Verify to obtain an actual owner verified result for Project, never constructs accepted result. Known admission/runner tooling-only cases explicitly listed in manifest-bound applicability artifact; unknown semantic-output operations fail closed. Source replay status and failure stablecode compared to original actual producer observation; Project source anchor/diagnostics checked independently. Full operation applicability (Encode/Migrate/etc) and extended replay are NOT finished.

CompositionRunner now supports separate --materialize-manifest --profile composition --corpus <root> --producer-envelope <file> --consumer-manifest <newfile> --source-root <GhostagramRoot>; run omits materialize and adds --manifest-digest <externallyFrozenDigest> --output <freshdir> [--source-revision <40hex>]. Both load/admit exact producer inputs and derive concrete inventory without executing cases during materialization. Source fixture bundle contains exact mapping+test+runner C# bytes in one hashed local artifact, avoiding repeated many-source hashes per observation. Applicability is separate local manifest artifact. Session verifies exact manifest bytes vs current inventory/source before execution, preserves actual nullable admission-only output, merges producer anchor fields, publishes only System-admitted consumer envelope and returns failure for any failed case. No full run/manifest/envelope generated yet.

Added test-only System Composition.Exchange project ref. Current new decoder/CLI source has NOT been built; last built conformance binary predates new decoder. Parent says System now owns shared build slot;105241Z superseded as buildinput. Extended decoder API still pending: RawProfile, ContextDescriptor deterministic clock/cancel/fault checkpoints, reference dispositions, compatibility/migration requests, explicit prerequisite anchors and BindEncodeRecord(actual prior record). Do NOT substitute normal profile/context for those real fixture controls. Extend only after actual APIs appear. No builds/tests until next explicit generation+slot.

CompileFailed fixture source has been corrected by agent to retain actual root and omit required child, so constructor is valid and actual compiler rejects inconsistent exports. CompileCollision unique IDs pass constructor and duplicate internal export target reaches actual compiler. These corrections not tested yet. Current source static XML paths and Python AST valid; git diff --check passes with line-ending warnings only.


## Source-only resumption and owner repair-cycle exhaustion

Parent reports final F004 repair cycle 2 independently Rejected: accepted typed metadata edit can be replaced by stale original opaque metadata during CaptureLocal/EncodeLocal. Earlier opacity/depth findings were fixed, but repair budget exhausted; parent requests human disposition. No additional Graph repairs, owner workaround, final delivery acceptance, builds/tests/restores permitted. Continue independent F003 source only. Frozen F004 local VALIDATION.md remains unchanged (SHA256 79df10793ce7231ed1c29ffcdd764522a5c80ed54a7813fd414a4cedb7e83c7c).

Actual owner test-only execution driver is ../ghostworx-system/tests/Ghostworx.System.Composition.Conformance/CompositionFixtureExecutionDriver.cs, namespace Ghostworx.System.Composition.Conformance.Runner. It constructs actual Compiler/Exchange/Migration with internal checkpoint probes, exact nullable Profile/Context, scheduled clock/cancellation/fault, observed checkpoints and AllScheduledCheckpointsObserved. Conformance executable now references this test-only owner project; production module does not. Bridge.Tests now references Composition.Exchange and calls OwnerDiagnosticProjectionTests.Run in composition mode. No build/test has run for these changes.

Mapper added five actual diagnostic-only Project overloads for Encode/Migrate/Inspect/Negotiate/GetExact and actual IR ReferenceProvenance copied into source anchors and equality. Sixth Decode overload is being added because separately executed Verify cannot substitute for the exact rejected Decode result. No fake compilation/verification result or IR anchor/Fresh. Mapper owns new CompositionOwnerReplay.cs plus existing module/test paths; root owns ProducerProjectionCases integration. Replay source uses explicit prerequisites/actual binding, owner driver, exact expectation/producer/checkpoint checks; accepted wire results get distinct actual verification. Shared owner observed-field projector is still pending, so it cannot claim replay readiness or semantic exclusion yet. GetExact standalone decoder variant remains unavailable.

Draft canonical F003 Design2 is now authored, not Accepted; accepted Design1 preserved byte-exact at DESIGN-REVISION-001.md (SHA256 1758b794d6cbcccba65d98b9f8d2dc16377db25a4ef68623f7bac627241eee8f). It records six typed diagnostic-only variants, ordered ReferenceProvenance, explicit source preparation and semantic-success/no-PublicView inapplicability distinct from tooling. Rejected/mismatched/unavailable/unreached-checkpoint cases remain required. Parent informed; independent Elon backtracking review awaits concrete source. Draft header, test project XML/reference paths, and git diff --check passed (line-ending warnings); no dynamic verification claimed.


## Final source preparation and initial Design2 review submission

Parent relayed Justin's explicit approval: "The request for another F004 repair cycle is approved." One additional bounded System-owned F004 repair/review cycle3 is authorized, preserving prior history. No consumer owner workaround, new acceptance, or local build permission is implied. System retains build slot; await exact generation manifest and grant. Earlier pending-human-disposition text is historical and now superseded by this authorization; delivery remains unaccepted.

Initial F003 Design2 successor review is separate from F004repaircycles. Reviewer requested only submission timestamp/historical locator corrections (not rejection; no repaircycle consumed). Corrected and held: Design2 1ad10b97765cf6647e8027e3dfdc8cb6cadb7641c4e5dc63214755fa4331b683; checks/design-revision-002-review/candidate-files.json f27fe0411043255bd1a70d76305899048ca8c40bdedc7eb04dc5e7f26d9c1afe; REVIEW-PACKET.md b7097a9ed729f5f02e4055bcdedf076387ce409c2b8a202fb4116feb8f1f43bc. Design Recorded submission2026-09-07T11:59:35Z/Pending, YAML historical F004Design1 archive, prose historical SystemF004Design2 archive now exact. No semantic change, no selfapproval. Previous submission packet/manifest retained adjacent SUBMISSION-001. Existing ModuleTarget2 already covers explicit rejected D3result inputs, so unchanged. All13candidate file hashes verified unchanged after integration edits outside candidate. Parent reviewing with elon_bootstrap.

CompositionOwnerReplay.cs now implements actual driver replay for seven owner request variants, explicit bounded/cycle-checked Compile/Decode prerequisites and actual BindEncodeRecord/CreateReferenceSource, exact raw nullable profile/context, real checkpoint receipts, exact expected status/ordered codes and producer stablecode. Owner observed-field contract still pending -> ReadyForProjection=false. It retains actual typed original result, optional separate actual successful wire-result Verify, IR/publicview only when actual accepted. Adapter invocation is deterministic UnixEpoch/25s/anchor correlation. Standalone GetExact remains unavailable while no actual decoder; no fabricated result. Agent narrow followup removes unnecessary Verify for rejected Decode; root passes rejected Decode directly to diagnostic-only Project.

ProducerProjectionCases.cs now explicitly prepares source replay before manifest, binds full deterministic receipts to applicability artifact and its digest in each required case, and invokes typed Project separately only through actual results. Exact matching semantic success without PublicView can become separately recorded semantic inapplicability only when all owner checks are complete; pending/mismatch/unsupported availability never removes required cases. Actual Project may execute with prepared source while observed fields pending, but Passed=false. Local runner now preserves per-case failure records and continues fixed inventory instead of losing its index at first exception. No owner methods inside Project execution.

Parent reports owner auditing14L03/L05 GetExact and21L23/L24/L25 ProducerEnvelope mapping drift against accepted Compile ownership. Treat current inventory as provisional; consume owner corrections when supplied, never infer child case-name mapping. No builds/tests/restore/current-generation envelope/visual evidence yet. Project XML/reference paths, diagnostic runner Python syntax, review file hashes, document link paths and git diff --check verified (line-ending warnings). All7F003 delivery criteria remain open.


## Independent F003 Design2 acceptance recorded

Parent supplied exact actual elon-musk (/root/elon_bootstrap_diagnosis) Accepted/APPROVED cycle0 no conditions at2026-09-07T12:01:36Z. Mechanically recorded exact JSON and verified readback in checks/design-revision-002-review/DECISION.json SHA55f072c8d07519202cd6d6b02b1134ce1756d5e49e4399911de55c3169a39bb9. Reviewed Design2 bytes preserved adjacent DESIGN-REVIEWED.md SHA1ad10b97765cf6647e8027e3dfdc8cb6cadb7641c4e5dc63214755fa4331b683. Current canonical Design2 metadata Accepted SHA cda8e37adadc65a3dbc584d39c43dd858937da368ec2763f9445a9c6f94cf937. Semantic content unchanged; historical submissions/review packet/manifest unchanged. No source/test/delivery acceptance, architecture promotion or F004reviewcounter effect. All7criteria unvalidated.

Agent replay helper now stable and does adapter Verify only for actual successful Decode/Encode/Migrate. Rejected Decode goes directly to actual typed diagnostic Project; no substitute Verify. Original producer stable-code mapping checked against actual owner runner diagnostics-empty ACCEPTED elsefirstcode. Helper still marks shared owner observed-fields Pending, readinessfalse; GetExact standalone provisional unavailable. Root inventory uses actual Project even when prepared fields pending but Passedfalse; cannot exclude pending/mismatchedsource as semanticnonprojectability. Await corrected owner registry/inventory, shared observer, newgenerationmanifest and explicit buildslot. No processes, builds/tests/restores running locally.

## Shared owner result observation integrated

System supplied `tests/Ghostworx.System.Composition.Conformance.Support/CompositionResultObservation.cs`, inspected at SHA256 `32fdaf8d0c0dec72a8636abe0824ca0873ca029b72a82b0c4bf56b871d6e89e0`. It exposes actual D3-only observations for Compile, Encode, Decode, Verify, Inspect, Negotiate and Migrate with the owner registry operation/schema identity, exact diagnostic order, cloned observed fields and status-sensitive required field names.

Ghostagram `CompositionOwnerReplay.cs` now consumes that shared observer for the root operation, every explicit adapter operation and every prerequisite. SHA256 after source integration is `1b038beca400733e19e4a2c638ba2b8aac25d83fd3267642668cf2200e5e3c98`. Replay readiness requires exact operation/schema/status/diagnostics, complete required-field inventory, both declared expectation field sets using owner-equivalent JSON comparison, exact original producer result bytes, checkpoint coverage, prerequisites and any required successful wire-output Verify. Receipts retain cloned observed fields and required names. The former pending flag is now false; a mismatch remains a required failed case and cannot become semantic inapplicability.

`ProducerProjectionCases.cs` now has its required Compiler namespace import. Test-only conformance references remain confined to `tests/Ghostagram.Graph.Conformance`; no production project references owner runner/support assemblies. Static `git diff --check` and all three affected test project XML parses passed with only existing line-ending warnings. No source was built or executed against the new observer. Await corrected owner inventory, reviewed cycle3 output generation, exact manifest and explicit build slot.

## Provisional 120942 build and local projection diagnostics

Parent granted the shared slot for exact System `provisional-20260907T120942Z`: immutable 25-output owner manifest SHA256 `d716b81b89c9cc5152e4a3b577ee5381056f52f3b88981b8ceacc236b92bb273`, owner source fingerprint `5a70b636b93f11b5a85eea82ffccf1900e3292fc26ce260f690634b159e657da`, source archive SHA256 `00591636d04d2acc04ff5f997f792c08ec288e4d3d2c670aeea3ef75541c4c68`. The adjacent immutable supplement for the nested SemanticLinks support output has SHA256 `2635a9b6b75a8e6027a8d58265a90e72bb6df9415eb3454bcd25b9d3ec182944`; it binds the original manifest/fingerprint, exact project and DLL, and confirms source files still equal. The audit runner now admits only that validated supplement while preserving all original manifest checks.

The scoped child-only Release build passed for Ghostagram.Bridge, Ghostagram.Bridge.Tests, Ghostagram.Graph.Conformance and Ghostagram.Layout.Verification. Three actual composition runs exposed and preserved narrow fixture failures before passing: unpinned local Definition edges (`CMP-REFERENCE-SKEW`), revision-pinned VariableType/Scope/Reference identities rejected by Variable contracts, then a source revision whose pinned root identity did not agree with its changed root revision. The fixture now pins composition identities at revision 1 by default, explicitly leaves VariableType/Scope/Reference canonical identities unpinned, and pins only the three Definition identities to the supplied changed source revision. Bridge.Tests and Graph.Conformance rebuilt successfully after each correction. No owner workaround or semantic contract change was made.

`provisional-120942-composition-04` passed the complete composition mode. `provisional-120942-local-projections-02` passed all 174 local cases and wrote 128 actual projection JSON files plus the index. Its child source fingerprint is `469a7d0ddf1d1390344da560c082772a3f827d06926e207e4f57a7ba1d6decfe`, source archive SHA256 `bc313adc5e3d1179327826b254c92f4fb0756a7a56c21fe0cdb72845ec552a11`, and local index SHA256 `da4e38761b73e69f0413b243c6c5e3ae48026521936a37079882240ef3f93e2`. All owner outputs and every copied System DLL, including the exact supplemented nested support DLL, matched before/after. No forced termination occurred.

The failed diagnostic phases remain preserved: `provisional-120942-composition-01`, `-02`, `-03`, and `provisional-120942-local-projections-01`. These runs are provisional API/local diagnostics only. The full corrected corpus, producer envelope, producer-linked consumer manifest/run, final combined generation, renderer previews/visible inspection, Evidence and independent Validation remain open. The slot was explicitly released to the parent with no subprocess left running.
